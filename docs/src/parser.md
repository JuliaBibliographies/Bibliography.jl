BibParser handles the actual source parsing, but `Bibliography.jl` exposes the
same functionality through `read_bibliography` and `write_bibliography`.

This page is intentionally short because the package is meant to feel like a
single entry point rather than a parser zoo.

```@contents
```

```@autodocs
Modules = [BibParser]
```
