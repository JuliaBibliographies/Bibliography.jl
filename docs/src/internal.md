BibInternal contains the canonical entry model and the lossless document types
used by `Bibliography.jl`.

This page exists mainly as a bridge for readers who want to understand how the
high-level API maps onto the shared data model.

```@contents
```

```@autodocs
Modules = [BibInternal]
```
